{
  "name": "screenshot-tool",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "clean": "rm -rf .parcel-cache dist",
    "dev": "npm run clean && parcel serve index.html",
    "build": "npm run clean && parcel build index.html --public-url ./ --no-scope-hoist && npx serve dist",
    "serve": "npm run build && npx serve dist"
  },
  "dependencies": {
    "fabric": "^5.5.1",
    "html2canvas": "^1.4.1"
  },
  "devDependencies": {
    "buffer": "^6.0.3",
    "parcel": "^2.10.0"
  }
}
